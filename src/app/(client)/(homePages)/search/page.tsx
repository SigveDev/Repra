import SearchPageComponent from "@/app/(client)/(homePages)/search/searchPageComponent";

function SearchPage() {
  return (
    <div className="w-full h-fit min-h-screen flex flex-col items-center justify-start">
      <SearchPageComponent />
    </div>
  );
}

export default SearchPage;
